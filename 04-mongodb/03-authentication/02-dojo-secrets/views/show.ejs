
<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Show</title>

        <!-- Bootstrap CSS -->
        <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet">

        <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
        <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
        <!--[if lt IE 9]>
            <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.3/html5shiv.js"></script>
            <script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
        <![endif]-->
    </head>
    <body>
        <h1 class="text-center">Post a comment on this Secret</h1>

        <div class="container">
            <h2 class="text-center"><%=secret.secret%></h2>
            <%if (secret.comments.length>0){%>
                <% for (let x=0;x<secret.comments.length;x++){%>
                <h3 class="text-center"><%=secret.comments[x].comment%></h3>
                <h5 class="text-center">Comment <%=x+1%></h5>
                <%}%>
            <%}%>
            <form action="/comment" method="POST" role="form">
                <legend>Post a Comment</legend>
                <div class="form-group">
                    <label for="comment">Comment</label>
                    <input type="text" class="form-control" id="comment" name="comment" placeholder="Comment">
                    <input type="hidden" name="id" id="inputid" class="form-control" value="<%=secret._id%>">
                    
                </div>
                <button type="submit" class="btn btn-primary">Submit</button>
            </form>
            <a href="/dashboard">Go back to the dashboard</a>
            
        </div>
        

        <!-- jQuery -->
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
        <!-- Bootstrap JavaScript -->
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    </body>
</html>
